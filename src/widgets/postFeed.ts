'use strict'

export default (data: any, props: any) => {
    // TODO get posts from database and show them from newest to oldest
    return {
        type: "text",
        value: "TODO Post Feed"
    }
}

